---
name: ntfy
version: 2.21.1
slug: ntfy
description: Send push notifications to your phone or desktop using PUT/POST
url: https://github.com/binwiederhier/ntfy
webui: http://[HOST]:[PORT:8095]
init: false
arch:
  - aarch64
  - amd64
map:
  - share:ro
ports:
  8095/tcp: 8095
ports_description:
  8095/tcp: HTTP port for ntfy server
schema:
  base_url: str?
  listen_port: int
  cache_duration: str
  attachment_enabled: bool
  auth_enabled: bool
  auth_default_access: list(read-write|read-only|write-only|deny-all)?
  timezone: str
  firebase_key_file: str?
  smtp_sender_addr: str?
  smtp_sender_from: str?
  smtp_sender_user: str?
  smtp_sender_pass: password?
options:
  base_url: ""
  listen_port: 8095
  cache_duration: 12h
  attachment_enabled: false
  auth_enabled: false
  timezone: UTC
startup: services
boot: auto
watchdog: tcp://[HOST]:8095/health
timeout: 30
